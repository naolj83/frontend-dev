/**
 * 5. Arrow Function
 */

const power = function(x){
    return x * x;
}

let numbers = [1, 2, 3, 4, 5];
numbers.forEach(function(number){
    proccess.stout.write('${number}:${power(number)}');
});